return {
  {
    "folke/snacks.nvim",
    opts = {
      explorer = {
        -- Show dotfiles (.env, .gitignore, ...) by default. Toggle
        -- per-session with <A-h> (hidden) / <A-i> (gitignored) while the
        -- explorer window is focused.
        hidden = true,
      },
    },
  },
}
